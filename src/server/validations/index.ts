import campaignValidation from './campaign.validation';
import subscriptionValidation from './subscription.validation';

const options = { keyByField: true };

export { subscriptionValidation, campaignValidation, options };
